# Core dependencies
browser-use>=1.0.0
langchain-openai>=0.1.0
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0
pyperclip>=1.8.0

# Additional dependencies that might be needed
asyncio-compat>=0.1.0
aiohttp>=3.8.0
selenium>=4.0.0

# Browser dependencies (Chrome/Chromium will be installed in Docker)
playwright>=1.40.0 